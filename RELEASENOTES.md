**1.3.1.1**
- merged goatcorp/1.3.1 back into RB-patched
- Updated localization for new entries in RB-patched (no crowdin yet, so it uses English fallback for RB-specific options)
- xlm install scripts now install along-side the official xlm release instead of replacing it.
- Added SDL3 patches from goatcorp/main
- Fixed scaling in X11 mode
- Global HappyEyeballs HttpClient, instead of creating a new one for each download operation
